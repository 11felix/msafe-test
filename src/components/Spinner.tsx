import React, { useEffect, useState } from "react";

const Spinner = () => {
    return (
        <div className="border-gray-300 h-[1.1vw] w-[1.1vw] animate-spin rounded-full border-[0.2vw] border-t-blue-600" />
    );
}

export default Spinner;